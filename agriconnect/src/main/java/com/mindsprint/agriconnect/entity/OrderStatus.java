package com.mindsprint.agriconnect.entity;

// Create an Enum for simplicity
public enum OrderStatus {
    DRAFT,
    PENDING,
    CONFIRMED,
    IN_TRANSIT,
    DELIVERED,
    CANCELLED,
    FAILED
}